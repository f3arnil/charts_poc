export default theme => ({
  root: {
    height: '100vh',
    overflow: 'hidden',
    alignItems: 'center',
    justifyContent: 'center',
    display: 'flex',
    flexDirection: 'column',
    textAlign: 'center',
    padding: theme.spacing.unit * 2,
  },
});
